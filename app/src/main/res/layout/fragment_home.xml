<?xml version="1.0" encoding="utf-8"?>

<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.pushpal.googleplaymodified.viewPager.CustomViewPager
        android:id="@+id/homeViewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/custom_sub_tab_layout_height"
        android:background="@color/colorPageGrey"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <android.support.design.widget.TabLayout
        android:id="@+id/sub_tabs"
        android:layout_width="match_parent"
        android:layout_height="@dimen/custom_sub_tab_layout_height"
        android:layout_gravity="bottom"
        android:background="@color/colorPrimary"
        app:elevation="10dp"
        app:layout_scrollFlags="scroll|enterAlways"
        app:tabGravity="fill"
        app:tabMode="scrollable"
        app:tabPaddingEnd="14dp"
        app:tabPaddingStart="14dp"
        app:tabSelectedTextColor="@color/colorPrimary"
        app:tabTextAppearance="@style/SubTabLayoutStyle" />
</android.support.design.widget.CoordinatorLayout>
